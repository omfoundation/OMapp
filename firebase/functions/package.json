{
    "scripts": {
        "test": "mocha --reporter spec"
    },
    "devDependencies": {
        "firebase-functions-test": "^0.1.2",
        "mocha": "^5.2.0"
    },
    "dependencies": {
        "firebase": "^5.2.0",
        "firebase-admin": "^5.12.1",
        "firebase-functions": "^1.1.0"
    }
}
